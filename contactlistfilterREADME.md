# businesstools
Make sure CSV you wish to filter has a column labeled "Title"


1. First specify the path of the file you wish to be filtered, then specify the export name.
2. Will display total number of contacts filtered
3. New CSV will export with file name specified before to the same folder as the .py
